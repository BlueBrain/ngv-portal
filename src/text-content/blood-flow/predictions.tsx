
const textContent = {
  pageDescriptionMeta: 'The simulation allows us to get outputs such as variation over time following a stimulation of many parameters such as blood vessels diameter, etc. We observe changes in flow per each time step of the astrocyte\'s model',

  pageDescription: (<p>The simulation allows us to get outputs such as variation over time following a stimulation of many parameters such as blood vessels diameter, etc. We observe changes in flow per each time step of the astrocyte's model</p>),
};

export default textContent;
